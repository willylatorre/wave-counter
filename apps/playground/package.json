{
  "name": "@waves-counter/playground",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "dev:fastapi": "uv run --project ../../python/wave-counter uvicorn server.fastapi:app --host 127.0.0.1 --port 18081",
    "dev:express": "npm run build --workspace @waves-counter/node && tsx server/express.ts",
    "prebuild": "npm run build --workspace @waves-counter/node && npm run build --workspace @waves-counter/client && npm run build --workspace @waves-counter/vue",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "pretest": "npm run build --workspace @waves-counter/node && npm run build --workspace @waves-counter/client && npm run build --workspace @waves-counter/vue",
    "test": "playwright test",
    "pretypecheck": "npm run build --workspace @waves-counter/node && npm run build --workspace @waves-counter/client && npm run build --workspace @waves-counter/vue",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@waves-counter/client": "0.2.0",
    "@waves-counter/node": "0.2.0",
    "@waves-counter/vue": "0.2.0",
    "express": "^5.1.0",
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@vitejs/plugin-vue": "^6.0.0",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.14",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vue-tsc": "^3.0.3"
  }
}
